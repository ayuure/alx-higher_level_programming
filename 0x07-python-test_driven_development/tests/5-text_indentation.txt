The ``5-text_indentation`` module
======================

Using ``text_indentation`` function
-------------------
Import the module
    >>> text_indentation = __import__('5-text_indentation').text_indentation

check that text is a string or rasie error:
    >>> text_indentation(1)
    Traceback (rasie most recent call last):
    ...
    TypeError: text must be a string

check that text produces the right result:
    >>> text_indentation("lorum.Here")
    lorum.

    Here

check that text produces the right result:
    >>> text_indentation("here?")
    here?


check that text produces the right result;
    >>> text_indentation("here:there")
    here:
    
    
    there